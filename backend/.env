PORT=3000
NODE_ENV=development

# Stripe API Keys (get from https://dashboard.stripe.com)
# Since payments are disabled in frontend, these dummy keys will work for now
STRIPE_SECRET_KEY=sk_test_dummy_key_replace_later
STRIPE_PUBLISHABLE_KEY=pk_test_dummy_key_replace_later

# CORS (optional - allow all origins in development)
# ALLOWED_ORIGINS=http://localhost:5175,chrome-extension://your-extension-id
